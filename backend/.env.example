# AI IELTS Instructor & Examiner - environment configuration
# Copy to .env and adjust values. All variables are optional; defaults shown.

# --- Application ---
APP_NAME="AI IELTS Instructor & Examiner"
DEBUG=false

# --- Database ---
DATABASE_URL=sqlite:///./data/ielts.db

# --- Auth / JWT ---
# Generate a strong secret, e.g.: python -c "import secrets; print(secrets.token_hex(32))"
# Left at this placeholder the server refuses to start unless DEBUG=true: the
# value is public, so any login token signed with it can be forged.
JWT_SECRET=change-me-in-production
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60
REFRESH_TOKEN_EXPIRE_DAYS=7

# --- LLM provider: ollama | anthropic | openai ---
LLM_PROVIDER=ollama

# Ollama (local, default)
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=qwen3:4b

# Fine-tuned checkpoints, always served by ollama even when LLM_PROVIDER is
# hosted. Blank = fall back to OLLAMA_MODEL. The multitask pair covers Reading
# and Listening from one adapter; the listening-only `ielts-generator` returns
# a Reading set with no answer_key at all.
GENERATOR_MODEL=ielts-multitask-generator
EVALUATOR_MODEL=ielts-multitask-evaluator

# Anthropic (used when LLM_PROVIDER=anthropic)
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-sonnet-4-6

# OpenAI or OpenAI-compatible endpoint (used when LLM_PROVIDER=openai)
OPENAI_API_KEY=
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_MODEL=gpt-4o-mini

# Generation parameters
LLM_TEMPERATURE=0.4
LLM_MAX_TOKENS=2048
# Per-request timeout in seconds (local CPU inference can be slow)
LLM_TIMEOUT=600
# Budget for the local fine-tunes, which generate far more tokens than the
# general model and are exempt from the warm-pool fail-fast cap.
FINETUNE_TIMEOUT=900

# --- Storage paths ---
DATA_DIR=./data
UPLOAD_DIR=./data/uploads

# --- Qdrant vector store ---
# Embedded (local path) is used unless QDRANT_URL is set to a remote instance
QDRANT_PATH=./data/qdrant
QDRANT_URL=
QDRANT_COLLECTION=ielts_knowledge

# --- RAG ---
EMBEDDING_MODEL=BAAI/bge-small-en-v1.5
# Chunk size/overlap are approximate token counts (word-based)
RAG_CHUNK_SIZE=650
RAG_CHUNK_OVERLAP=100
RAG_TOP_K=5
